%This is a very simple one as we don't need to import depths or bins (which
%is done in preprocessing) or truncate the record for sidelobe data.
burstBeamVelocities = importSynthBurst(paramStruc.dataLocation,burstCtr);